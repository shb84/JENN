channels:
- conda-forge
- defaults
dependencies:
  - python >=3.11,<3.12
  # build
  - doit[toml]
  - conda-lock
  - pip 
  # runtime
  - numpy 
  - orjson 
  - matplotlib-base
  # protyping
  - jupyterlab >=3.6,<4.0.0a0
  # test
  - pytest
  - pytest-cov
  - pytest-html
  # docs 
  - sphinx 
  - sphinx_rtd_theme
  - tectonic 
